#!/bin/bash

eval $(free -m | grep Swap | awk '{print "total="$2"; used="$3"; free="$4";"}')

repeat() {
    for i in `seq $2`; do echo -n "$1"; done
}

used=$[8 * used / total]
free=$[8 * free / total]

repeat "$(echo -e '\xb7')" $used
repeat " " $free
echo
